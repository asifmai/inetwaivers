<div class="container regularguest-tab index-tab
<% if (page == 'loginpage') { %>
  active
<% } %>
">
  <div class="row">
    <div class="col-md-5 mx-auto">
      <h2 class="h2 h2-responsive text-center underlined-heading">Regular Guest</h2>
      <form action="/users/login" method="POST" data-parsley-validate>
        <div class="row">
          <div class="col">
            <div class="md-form">
              <input type="email" id="materialLoginFormEmail" class="form-control text-white" data-parsley-required name="email"
              <% if (typeof email != 'undefined') {%>
                value="<%= email %>"
              <% } %>
              >
              <label for="materialLoginFormEmail" class="text-white">E-mail</label>
            </div>
          </div>
        </div>
        <div class="row">
          <div class="col">
            <div class="md-form mt-0">
              <input type="password" id="password" class="form-control text-white" name="password" data-parsley-required data-parsley-minlength="6">
              <label for="password" class="text-white">Password</label>
            </div>
          </div>
        </div>
        <button class="btn btn-primary btn-rounded btn-block my-4 waves-effect" type="submit">Continue</button>
        <p class="text-center">Not a Rugular Guest?
          <a href="" class="btn-newguest">Register</a>
        </p>
      </form>
    </div>
  </div>
</div>